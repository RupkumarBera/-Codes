#include<stdio.h>
#include<conio.h>

    int main ()
    {
        float num,absnum;
        printf ("Enter the number:");
        scanf("%f",&num);
        if (num>=0)

        {
            printf(" Number is absolute");

        }
        else
        {
            printf("Number is not absolute");
        }
        return 0;
    }

